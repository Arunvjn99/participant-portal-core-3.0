# Copy to .env or use with .env.development / .env.staging / .env.production
# Required for Supabase auth and data. Get values from Supabase → Settings → API.

VITE_ENV=development
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_key
