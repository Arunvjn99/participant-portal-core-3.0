/**
 * Loan application flow. Re-exports the full loan flow from applications.
 * Uses same stepper and footer CTA pattern as enrollment.
 */
export { LoanFlow as default } from "../applications/LoanFlow";
