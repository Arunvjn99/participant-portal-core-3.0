/**
 * Legacy import path: delegates to `VersionedEnrollment` (v1 when no `version` param).
 * Legacy v1 shell: `src/archive/enrollment-v0/`; v2: `src/versions/v2/enrollment/`.
 */
export { VersionedEnrollment as EnrollmentLayout } from "@/pages/enrollment/VersionedEnrollment";
